# Generated by `yarn storybook:fixtures` — committed but not hand-edited, so the
# formatter must leave the generator's JSON output alone (avoids churn). oxfmt
# reads this file. The barrel index.ts is generated too.
tools/storybook/fixtures/
