# -------------------
# NEXT
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# -------------------
# SANITY
NEXT_PUBLIC_SANITY_PROJECT_NAME="Loglee Wedding"
NEXT_PUBLIC_SANITY_PROJECT_ID=""
NEXT_PUBLIC_SANITY_DATASET="production"
NEXT_PUBLIC_SANITY_API_VERSION="2021-03-25"
SANITY_API_READ_TOKEN=""
SANITY_WRITE_TOKEN=""

# -------------------
# WEBHOOKS & DEPLOYMENT
SANITY_WEBHOOK_SECRET=""
NETLIFY_BUILD_HOOK_URL=""

# -------------------
# GUESTS — the guest sheet and the site gate (proxy.ts)
# A Google service account with the sheet shared to it as Editor. Keep the key's \n newlines.
GOOGLE_SERVICE_ACCOUNT_EMAIL=""
GOOGLE_PRIVATE_KEY=""
GUEST_SHEET_ID=""
# Signs the guest cookie. Any long random string (`openssl rand -base64 32`). Unset = nobody can enter.
GUEST_SESSION_SECRET=""
# Lets the couple in from the entry page without a guest ID. Unset = no password works.
COUPLE_PASSWORD=""
# Loops — sends the invitation and reminder emails (tools/guests/loops.ts)
LOOP_API_KEY=""

# -------------------
# GOOGLE MAPS
# Browser key, restricted by HTTP referrer. Needs the Maps JavaScript, Places and Static Maps APIs
# (Places and Static Maps are for the Studio's location picker).
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=""
# Optional Cloud map ID. Leave unset to use the site-coloured JSON style in components/Map/mapStyle.ts;
# set it and the Cloud console's style replaces that one.
# NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID=""

# -------------------
# ANALYTICS & TRACKING
NEXT_PUBLIC_GOOGLE_TAG_MANAGER_ID="GTM-XXXXXX"
# NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=""
# NEXT_PUBLIC_HUBSPOT_ID=""
# NEXT_PUBLIC_SEGMENT_ID=""
# NEXT_PUBLIC_FACEBOOK_PIXEL_ID=""
# NEXT_PUBLIC_HOTJAR_ID=""

# -------------------
# CLAUDE CODE / MCP
FIGMA_PERSONAL_ACCESS_TOKEN=
LINEAR_TEAM_ID="MAM"
LINEAR_PROJECT_ID=""
SLACK_COLLAB_CHANNEL_ID=""
